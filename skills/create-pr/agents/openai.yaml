interface:
  display_name: "Create PR"
  short_description: "Draft PR content from branch diffs and create PRs with gh."
  default_prompt: "Use $create-pr to inspect diffs, write .pr_description.md, and run gh pr create."
