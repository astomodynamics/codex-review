interface:
  display_name: "Create Commit"
  short_description: "Generate and run commit commands from real diffs."
  default_prompt: "Use $create-commit to inspect diffs, craft a commit message, and run git commit."
